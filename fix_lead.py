"""Fix 组长 summary to reflect team structure."""
from docx import Document

doc = Document('学生管理系统实验报告_更新版.docx')
t1 = doc.tables[1]

lead_summary = """实践总结（宫士淇——组长）：

在本课题中，我担任组长，全面负责项目总体架构设计、主循环菜单编写、文件输入输出模块（LoadFromFile、SaveToFile）以及最核心的特殊排序与数据统计导出模块（SortAndExport）的开发工作。

首先，我设计了整个系统的模块架构。考虑到课题要求使用散列函数存储数据，我选择了散列表的链地址法作为核心存储结构——以除留余数法 HashFunc(id)=id%13 计算散列地址，每个桶用单链表解决冲突。所有数据结构和操作函数封装在头文件 hash_table.h 中（共337行代码），主程序 student_management.cpp（约289行）负责用户交互界面调度。这种"数据层+界面层"的分离设计使代码结构清晰、易于维护。

在主循环菜单模块中，我实现了清晰的7选项控制台菜单界面，main函数放在文件顶部便于整体把握程序逻辑。为提升用户体验和健壮性，加入了输入验证函数（readInt、readFloat 带类型检查与错误恢复）和 clearInput 缓冲区清理函数。调试过程中发现当使用 echo 管道测试时，cin 到达 EOF 后 failbit 被置位导致死循环——通过增加 cin.eof() || cin.fail() 检测成功解决。

文件I/O模块实现了程序启动时自动加载和退出时自动保存的双向持久化机制，8条样例数据存储在 records.txt 中，格式为：学号 姓名 性别 年龄 成绩（空格分隔）。

排序统计导出模块是本课题的核心难点。由于散列表本身无序，我设计了"提取→排序→统计→导出"四步走策略：先用动态数组收集所有学生记录，再用冒泡排序按性别升序（男0在前女1在后）+成绩降序的复合条件排序，接着遍历统计男女合格率（≥60分为合格）和全班平均分，最后将结果同时输出到控制台和 sorted_output.txt 文件。测试结果表明排序正确（男生4人按成绩81→75→74→68排列，女生4人按成绩72→64→58→55排列），统计准确（男生合格率100%，女生50%，全班平均68.38）。

在团队协作方面，我负责整体进度的把控和代码整合。王逸轩同学负责基础数据管理模块（Insert插入、TraverseTable浏览），俞皓译同学负责数据维护与检索模块（Delete删除、SearchById/SearchByName双向查询、Update修改）。我将各模块整合到统一的主菜单框架中进行联调测试，确保所有功能协调运行。

调试过程中遇到的主要技术问题：
（1）散列函数命名冲突：最初命名为 hash，与 C++ 标准库 std::hash 模板类冲突导致编译失败（g++ 报 "reference to 'hash' is ambiguous"），解决方案是将函数改名为 HashFunc。
（2）管道测试死循环：echo "5" | ./program 导致无限循环，原因是 cin 到达 EOF 后未检测，通过增加 cin.eof() || cin.fail() 检测并在检测到EOF时 break 解决。
（3）Update函数学号变化处理：若用户修改学号导致散列地址变化，简单的原地修改会破坏散列表结构，需要采用"先Delete旧记录再Insert新记录"的策略，同时检查新学号是否已存在。

通过本次课程设计，我深入理解了散列表的存储原理与链地址法实现，掌握了模块化程序设计的完整流程（需求分析→数据结构设计→函数接口定义→编码实现→测试验证→文档撰写），同时也锻炼了团队分工协作与代码整合的能力。不足之处在于排序算法采用了O(n²)的冒泡排序，在数据量极大时效率偏低，后续可优化为快速排序或直接使用STL的sort；另外散列表大小固定为13，未实现动态扩容（rehashing）机制，这是可以进一步改进的方向。"""

t1.cell(4, 2).text = lead_summary

doc.save('学生管理系统实验报告_更新版.docx')
print('Lead summary updated!')
